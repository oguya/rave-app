<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.droid.raveapp" >

    <!-- openGL support -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- api key -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBsaT0xHf2H4_mdHm9T9x3wNa4KbQ779SU" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="com.droid.raveapp.ui.MainActivity"
            android:label="@string/app_name" >

        </activity>

        <!-- plan trip -->
        <activity
            android:name="com.droid.raveapp.ui.PlanTrip"
            android:label="@string/title_activity_plantrip" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- view stats -->
        <activity
            android:name="com.droid.raveapp.ui.ViewStats"
            android:label="@string/title_activity_view_stats" >
        </activity>

        <!-- search stage -->
        <activity
            android:name="com.droid.raveapp.ui.SearchStage"
            android:label="@string/title_activity_search_stage" >
        </activity>
        <activity
            android:name="com.droid.raveapp.ui.RaveApp"
            android:label="@string/title_activity_rave_app" >
        </activity>
    </application>

    <!-- permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

</manifest>
